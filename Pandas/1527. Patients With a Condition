import pandas as pd

def find_patients(patients: pd.DataFrame) -> pd.DataFrame:
    regex = re.compile(r'(^| )DIAB1')
    return patients[patients["conditions"].str.contains(regex)].sort_values(by="patient_id")
